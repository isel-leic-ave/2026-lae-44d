package pt.isel

// Destiny class sample.
// The parameter country (the property with different name) must be optional here,
// with an initialized value.
class Artist(val name: String, val country: String = "", val kind: String)